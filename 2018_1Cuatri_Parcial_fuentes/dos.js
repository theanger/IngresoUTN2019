function mostrar()
{
    var nomb;
    var local;

    nomb= document.getElementById("elNombre").value;
    local= document.getElementById ("laLocalidad").value;

    alert("Su nombre es " + nomb + "y vive en " + local);
  
}
